[phases.setup]
nixPkgs = ['nodejs_22', 'pnpm-10_x']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm --filter @cm316/web build']

[start]
cmd = 'pnpm --filter @cm316/web start'
